global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yaml

scrape_configs:
  - job_name: stockstudy
    static_configs:
      - targets: ["host.docker.internal:9091"]  # pushgateway / app metrics endpoint
